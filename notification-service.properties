spring.boot.message=Notification Service

# R2DBC & Pool Settings
spring.r2dbc.url=r2dbc:pool:mariadb://localhost:3306/notification_db?initialSize=5&maxSize=20&maxIdleTime=PT30M&validationQuery=SELECT%201
spring.r2dbc.username=root
spring.r2dbc.password=root

eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

management.endpoints.web.exposure.include=*

management.tracing.sampling.probability=1.0
management.tracing.enabled=true
management.tracing.propagation.type=w3c

eureka.instance.prefer-ip-address=true
eureka.instance.hostname=localhost

logging.file.path=D:/AY/Work/Projects/Java/Logs
logging.file.name=EmailService
logging.pattern.level=%5p [${spring.application.name:-},%X{traceId},%X{spanId}]

spring.messages.basename=ValidationMessages

spring.reactor.context-propagation=AUTO
#################################################################################################

# Kafka - unified bootstrap property
spring.kafka.bootstrap-servers=localhost:9092

# Consumer-specific
spring.kafka.consumer.group-id=email
spring.kafka.consumer.auto-offset-reset=latest

# Topic names
spring.kafka.topic.name=order_topics
spring.kafka.dlq.topic=email_dlq

# Timeouts / tuning (optional, tweak as needed)
app.email.max-retries=3
app.email.send-timeout-seconds=10
app.email.retry-backoff-ms=2000
app.consumer.shutdown-wait-ms=30000