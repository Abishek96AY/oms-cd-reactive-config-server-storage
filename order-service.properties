spring.boot.message=Order Service

# R2DBC & Pool Settings
spring.r2dbc.url=r2dbc:pool:postgresql://postgresql:5432/order_db?initialSize=5&maxSize=20&maxIdleTime=PT30M
spring.r2dbc.username=postgres
spring.r2dbc.password=root
spring.r2dbc.properties.ssl=false
spring.r2dbc.properties.applicationName=${spring.application.name}

# Flyway
spring.flyway.enabled=true
spring.flyway.url=jdbc:postgresql://postgresql:5432/order_db
spring.flyway.user=postgres
spring.flyway.password=root
spring.flyway.driver-class-name=org.postgresql.Driver
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration
spring.flyway.validate-on-migrate=true
spring.flyway.clean-disabled=true

eureka.client.service-url.defaultZone=http://service-registry:8761/eureka/

management.endpoints.web.exposure.include=health,metrics,prometheus,circuitbreakers
management.endpoint.health.show-details=always

management.metrics.export.prometheus.enabled=true
management.metrics.tags.application=${spring.application.name}

management.tracing.sampling.probability=1.0
management.tracing.enabled=true
management.tracing.propagation.type=w3c

management.zipkin.tracing.endpoint=http://zipkin:9411/api/v2/spans

eureka.instance.prefer-ip-address=true
#eureka.instance.hostname=localhost

# For Logs
logging.file.path=/app/logs
logging.file.name=ProdOrderService
logging.app.name=${spring.application.name}

spring.messages.basename=ValidationMessages

spring.reactor.context-propagation=AUTO

# Swagger
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=false
springdoc.api-docs.path=/v3/api-docs

# Kafka bootstrap
spring.kafka.bootstrap-servers=kafka:9092

# Topic names
spring.kafka.topic.name=order_topics
spring.kafka.dlq.topic=order_dlq

# Producer send retry / timeout tuning
app.producer.send-timeout-ms=10000
app.producer.max-retries=3
app.producer.retry-backoff-ms=200